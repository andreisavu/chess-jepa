"""
Handcrafted tokenizer for the language of the project. This is as minimal as it can be for the game of chess.
"""

def encode(input: str) -> list[int]:
    """
    Encodes the input string into a list of tokens.
    """
    return []

def decode(input: list[int]) -> str:
    """
    Decodes the list of tokens into a string.
    """
    return ""
